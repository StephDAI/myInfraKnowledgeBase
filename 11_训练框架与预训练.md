# 11 训练框架与预训练

> 来源：`Diary/Megatron-LM笔记.md`（9.15）
> 关联：[10 训练基础](./10_训练基础.md)、[06 分布式并行](./06_分布式并行.md)（Megatron 的 TP/SP 布局转换与 `CUDA_DEVICE_MAX_CONNECTIONS`）、[03 模型与算法](./03_模型与算法.md)（LoRA 等下游适配手段）

---

## 1. 迁移学习（Transfer Learning）

**定义**：把模型在**大规模通用语料上预训练学到的知识（神经表示）**，搬过来用到某个具体的**下游任务**（如分类、问答、翻译）上，而**不是让下游任务从零开始学习**。

- **迁移对象的演进**（越来越「本体化」）：
  1. **静态词向量**（如 Word2Vec/GloVe）——迁移的是固定的词表示；
  2. **上下文表示**（如 ELMo/BERT 类预训练表示）——迁移的是随上下文变化的表示；
  3. **语言模型本体**——直接把整个预训练语言模型迁移到下游。

- **价值**：下游任务无需从零训练，用少量标注数据即可复用大规模预训练得到的通用表示。

---

## 2. 预训练与 Megatron-LM

- **预训练（Pre-training）**：在大规模通用语料上从零训练语言模型本体，得到可迁移的通用表示（即 §1 被迁移的对象）。
- **Megatron-LM**：NVIDIA 的大模型预训练框架。正如 `Diary/Megatron-LM笔记.md` 所记，它**解决的核心问题就是「如何预训练更大的模型」**——手段是大规模分布式并行（TP/PP/DP/SP）。并行策略本身见 [06 分布式并行](./06_分布式并行.md)。

**知识库中 Megatron-LM 的落地痕迹**：

- **SP ↔ TP 布局转换**源自 Megatron：`--sequence-parallel` 只是一个开关（没有 `--sequence-parallel-size`），因为 SP 复用 TP 组（见 [06 §1.2 / §1.3](./06_分布式并行.md)）。
- **`CUDA_DEVICE_MAX_CONNECTIONS=1`**：强制 compute 与 communication 按确定顺序串行，保证 pipeline parallelism 正确性，是 **Megatron-LM 训练经典优化**（见 [06 §7](./06_分布式并行.md)）。

---

## 3. PetaFLOPs（算力度量单位）

- **PetaFLOPs/秒（PFLOP/s）**：每秒**千万亿（$10^{15}$）次**浮点运算，用于衡量训练 / 推理的算力或吞吐规模。
- 与计算量估算的关系：单层 matmul 及前向/反向 FLOPs 见 [10 §4](./10_训练基础.md)；算术强度与屋顶模型（用 FLOP/Byte 判断瓶颈类型）见 [04 §7](./04_性能分析方法论.md)。

---

## 4. 与知识库其它主题的关系

- **预训练 → 迁移学习 → 下游微调**是一条主线：预训练产出通用表示（§2），迁移学习把表示复用到下游（§1），而 [03 §5.1](./03_模型与算法.md) 的 **LoRA** 是低成本下游适配的一种具体手段。
- Megatron-LM 提供的分布式并行能力（TP/PP/DP/SP）是 [06 分布式并行](./06_分布式并行.md) 中多项技术的工程来源。
