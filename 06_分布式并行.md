# 06 分布式并行

> 来源：`手记.md` 6.22、6.25、7.3、7.16、7.19
> 关联：[02 推理框架与引擎](./02_推理框架与引擎.md)、[07 互连与网络技术](./07_互连与网络技术.md)、[08 DeepEP与MoE通信算子](./08_DeepEP与MoE通信算子.md)

---

## 1. 五种并行策略（❓"三种并行是什么意思"的完整解答）

> 记录 6.25 的「搞清楚：三种并行是什么意思」——实际展开为五种，这里统一给出。

| 并行                           | 切分对象             | 作用                          | 特点                   |
| ---------------------------- | ---------------- | --------------------------- | -------------------- |
| **DP（Data Parallelism）**     | 数据               | 不同请求/样本给不同 GPU              | 提高并发                 |
| **SP（Sequence Parallelism）** | 请求 Sequence      | 一个请求的不同 token 给不同 GPU       | 提高长上下文处理能力           |
| **CP（Context Parallelism）**  | Sequence + 上下文维度 | 把 Attention 的上下文维度也切到多张 GPU | 每张卡只持有一部分 KV         |
| **TP（Tensor Parallelism）**   | 参数               | 每个 GPU 算一部分                 | 单层内参数切分              |
| **PP（Pipeline Parallelism）** | 层                | 每个 GPU 算一组 layer            | 形成流水线，层间只需传中间激活，通信量少 |

> DP 训练侧的显存优化（ZeRO：把梯度/优化器状态/参数分片到各卡）见 [10 训练基础](./10_训练基础.md) §3。

**SP 详解**：用于**不需要一个 token 看到其他 token** 的逐 token 模块，如：
- LayerNorm
- Dropout
- residual
- MLP/FFN 的部分中间激活
- loss 等逐 token 操作

**CP 详解**：把 Attention 的**上下文维度**也切到多张 GPU 上，使每张 GPU 只持有一部分 KV，同时通过通信让 Query 能访问完整上下文。

**SP vs Flash-Decoding 的区分**（7.19）：
- **Flash-Decoding**：通过 KV 维度的切块（Split-K）提升 decode 阶段 SM 利用率；
- **SP**：解决「单卡显存塞不下超长上下文」，一个 GPU 只存一部分 token 的 cache。

### 1.1 SP 与 CP 的矩阵级理解

**① 三个轴与「context 维度」**

一个 token 块的激活是 `[b, s, h]`：B=batch，**S=序列/上下文轴**，H=隐藏维。SP、CP 都动 S 轴。「context 维度」就是 attention 里 K/V 的那个 S 轴：

```
Q[s,d]、K[s,d]、V[s,d]
Scores = Q·Kᵀ : [s, s]   ← 第 2 维 = context（每个 query 能看到哪些 key）★
P = softmax(Scores) : [s, s]
O = P·V : [s, d]
```

**「切 context 维度」= 沿 K、V 的 S 轴切开**，每张卡只存一段 context 的 K/V。

**② SP 切什么：只在「逐 token 区」切序列，不进 attention**

一层在两类子层间交替：

- **逐 token 区**（LayerNorm / dropout / residual / MLP 激活）：每 token 独立，切了无需通信；
- **跨 token 区**（attention / 矩阵乘）：有跨 token 依赖，切了必须通信。

**SP = 只在逐 token 区把 activation 按 S 轴切开**；进 attention/MLP 前会 **all-gather 拼回全长 S**。所以 **SP 里 attention 仍看全长序列**，它切的是 H 轴（头），不是 S 轴。

**③ CP 切什么：把 context 轴持续切进 attention**

**CP = 把 S 轴一直切进 attention 内部**，每卡只持 `1/cp` 段 token 的 Q/K/V（**含 KV cache**）。切完单卡只算局部：

```
卡 r：Q_r[s/cp, d] · K_r[s/cp, d]ᵀ → [s/cp, s/cp]   ← 只覆盖自己这段 context
```

要看完整 context，需 **ring attention**（K/V 块在环上轮转 + online softmax）或 **all-gather KV / Ulysses all-to-all**。

**④ 为什么 SP 省 activation 显存：纯 TP 的冗余 vs SP 的切片**

**纯 TP（不开 SP）**：row-parallel 的 all-reduce 把结果还原成完整 `[b,s,h]` 并在每卡复制一份；贴着边界的逐 token 算子就在这份**复制激活**上运行——**每卡存同样的完整激活、各算一遍相同的逐 token 计算（冗余 ×tp）**。

**TP+SP**：把 all-reduce 换成 **reduce-scatter**，每卡只留 `1/tp` 段序列，进矩阵乘前再 all-gather 拼回。以 `b=1, s=8192, h=4096`、fp16、tp=8 为例：

| 逐 token 区每卡一份激活 | 形状 | 每卡元素 | 每卡显存 |
| --- | --- | --- | --- |
| 纯 TP（复制） | `[1, 8192, 4096]` | 33.55 M | 67.1 MB |
| TP+SP（切开） | `[1, 1024, 4096]` | 4.19 M | 8.4 MB |

> 纯 TP 下逐 token 区要求每卡结果逐元素一致（否则复制副本发散），dropout 靠 **model-parallel RNG** 保证各卡用同一张 mask。SP 的收益以**省显存为主、省冗余计算为辅**（逐 token 算子 FLOPs 本就少）。

**⑤ SP vs CP 对比**

| | SP | CP |
| --- | --- | --- |
| 切 S 轴的哪段 | 只在逐 token 区 | attention 内部（K/V 的 context 轴） |
| 进 attention 时 S 是否拼回 | 是（all-gather 回全长） | 否（保持切开） |
| 每卡 attention 覆盖的序列 | 全长 s | 只 s/cp |
| 省的东西 | 逐 token 区 activation（÷tp） | **KV cache（÷cp）** + attention 计算 |
| 通信 | 边界 all-gather + reduce-scatter（量小） | ring / all-gather / all-to-all（KV 量级，大） |
| 是否独立并行度 | 否，复用 TP 组 | 是，独立 group/size |

一句话：**SP 在「不需要互相看」的地方按 S 切开省 activation；CP 在「必须互相看」的地方按 context 轴切开省 KV。**

### 1.2 SP 与 CP 的组合

**① 逐 token 区 SP×CP**

逐 token 区里 SP 与 CP **同时切序列、相乘叠加**：CP 先粗切，SP 在 CP 段内再细切，每卡只对 `s/(sp·cp)` 个 token 做逐 token 计算。

```
序列 s=8192, sp=2, cp=2 → 切成 4 段
cp=0: token[0 .. 4095]        cp=1: token[4096 .. 8191]
   │ SP 再切                     │ SP 再切
sp=0:[0..2047] sp=1:[..4095]  sp=0:[..] sp=1:[..]
```

| 区段 | 生效的切分 | 每卡持有序列长度 |
| --- | --- | --- |
| 逐 token 区 | SP × CP | `s/(sp·cp)` |
| attention 区 | CP（TP 只再切头） | `s/cp` |

**② 维度 vs 布局：为什么 SP 复用 TP 组、CP 独立分组**

把激活看成一个 `[s, h]` 矩形，TP 与 SP 是它的两种切法，**份数都是 tp，rank 集合天然相同**：

```
   h ──────────────▶
 ┌────┬────┬────┬────┐
s│ 0  │ 1  │ 2  │ 3  │   ← TP：竖切（列条带）[s, h/tp]
↓├────┼────┼────┼────┤
 │ 0  │ 1  │ 2  │ 3  │
 └────┴────┴────┴────┘

┌────────────────────┐
│         0          │   ← SP：横切（行条带）[s/tp, h]
├────────────────────┤
│         1          │
├────────────────────┤
│         2          │
└────────────────────┘
```

- **SP 是 TP 同一根轴上的另一种「布局」**：同一时刻张量要么 TP 布局、要么 SP 布局，靠 collective 在两布局间转换 → 天然复用 TP 组。SP 必须搭 TP（没有 H 切分就没有可转换的目标布局；Megatron 里只有 `--sequence-parallel` 开关，没有 `--sequence-parallel-size`）。
- **CP 是另一根正交轴**（设备网格的一行），有独立 group 与 size，通信是 KV 交换 → 独立分组。若让 SP、CP 共用同一根轴，就只剩**一级**序列切分，丢掉 `tp×cp` 的并行度。

### 1.3 SP ↔ TP 的布局转换

**① 钥匙：all-reduce = reduce-scatter + all-gather**

TP 的 all-reduce 内部本来就含这两半。**SP = 把这两半拆开**：reduce-scatter 留在 row-parallel 后，all-gather 挪到下一个 column-parallel 前，中间夹住的逐 token 区顺势变成序列切片。因为是拆同一个 all-reduce，所以 **sp 必然 = tp**（通信总量也几乎不变）。

**② 转换 f：SP → TP（进 attention/MLP 前）**

1. **all-gather 序列**（TP 组内）：每卡拿到完整 `[s, h]`；
2. **取自己的 hidden 列片**（本地切片）：得 `[s, h/tp]`。

这一步融进 column-parallel 的输入（权重按输出维切，每卡 `W:[h, h_out/tp]`，XW后自然得到`[s, h_out/tp]`）。

**③ 转换 g：TP → SP（row-parallel 之后）**

**为什么是 row-parallel**：上一步 column-parallel 已把中间维 $h'$ 分片，下游 $B$ 为避免先拼回，就**按输入维（行）切**权重，每卡算局部和：

$$Y\cdot W_B = [Y_0\mid Y_1]\begin{bmatrix}W_B^0\\ W_B^1\end{bmatrix} = Y_0W_B^0 + Y_1W_B^1$$

**g 一次做两件事**：①跨 rank 求和（补全 row-parallel 欠的隐藏维规约）；②把序列按行散射回各卡 —— 这正是 **reduce-scatter**（先跨 rank 逐元素求和，再把结果按序列轴切成 tp 块）。

数值例（`s=4, h=2, h'=2, tp=2`）：

```
rank0: Z₀ = Y₀·W_B⁰          rank1: Z₁ = Y₁·W_B¹
  t0 [ 3   6]                  t0 [ 3   4]
  t1 [ 7  14]                  t1 [ 9  12]
  t2 [11  22]                  t2 [15  20]
  t3 [15  30]                  t3 [21  28]
            └────── 求 和 ──────▶ t0[6,10] t1[16,26] t2[26,42] t3[36,58]
reduce-scatter（rank0 留上半 t0,t1；rank1 留下半 t2,t3）:
  rank0: t0[6,10] t1[16,26]    rank1: t2[26,42] t3[36,58]
```

底层只交换一次（tp−1 步）：rank0 发下半、收上半并相加；rank1 反之。输出即 SP 布局 `[s/tp, h]`，可直接进下一层逐 token 区。

> 一般 tp：ring reduce-scatter，tp−1 轮，每轮把第 `(r−i) mod tp` 块发给下一张卡、收上一张卡的块累加，走完卡 r 手里第 r 块已是全量规约结果。

**④ 为什么 SP 与 TP 必须同 size**

| 转换 | 输入切分 | 输出切分 | 组 |
| --- | --- | --- | --- |
| f（all-gather） | tp 个序列块 | tp 个隐藏块 | TP 组 |
| g（reduce-scatter） | tp 份部分和 | tp 个序列块 | TP 组 |

输入块数、输出块数、参与 rank 数三者必须同为 `tp`。若 sp≠tp，就不是简单 all-gather/reduce-scatter，而要跨两组一般 all-to-all —— 那正是 CP 的做法（KV 交换，跨独立组，故 size 可不同）。

---

## 2. 专家并行（EP）与 EP+TP

**EP（Expert Parallelism）**：把 MoE 的专家切分到多张卡，每张卡只驻留一部分专家；token 通过 **all-to-all** 路由到对应专家所在卡（详见 [08](./08_DeepEP与MoE通信算子.md)）。

**EP+TP**：在 1 个专家下面再做 TP（即同一组专家内部再按参数切分）。

---

## 3. tp/ep/dp size 同时设置的含义（❓答疑）

> 记录 7.16：`tp-size 16、ep-size 16、dp-size 16` 同时设置。

**关键公式**：

- **`attn_tp_size = tp_size // dp_size`**。此处 = 1，即 **16 路 DP**：每张卡持有**完整注意力权重**，独立处理自己的请求批次，**不做 TP all-reduce**。
- **`attn_dp_rank = tp_rank // attn_tp_size`**（`rank` 可理解为编号）。
- **`moe_ep_rank = tp_rank // (tp_size // ep_size)`**
  - `tp_size // ep_size` = **一个 EP 组内有几张卡**（几个 TP rank 共享同一组专家）；此处 16 张卡**各持有一份不重叠的专家子集**。

一句话：注意力走 DP（无 TP all-reduce），MoE 走 EP（16 路各持不重叠专家子集）。

---

## 4. scale-up vs scale-out

- **scale-up**：使一台机器更强，单机多 GPU，NVLink/NVSwitch 域。
- **scale-out**：加更多机器。

---

## 5. 通信逻辑层级

```
框架层：PyTorch
通信库层：NCCL、MPI、UCX
GPU kernel / device 层：NVSHMEM、NCCL device API、multimem（通信写进 kernel 中）
```

- **multimem**：一个「特殊地址」同时代表多块 GPU 上的多份物理内存；对这个地址执行特殊指令时会同时作用到这些内存，属于 **scale-up 通信域**。

---

## 6. 集合通信原语

- **collective（集合通信）**：由一组 GPU（或进程）共同参与的一类通信操作，如 all-reduce。
- **all-reduce**：把多个节点（rank）的数据**汇总规约（求和/平均等）再分发广播回所有节点**。
  - 内部可拆解为 **reduce-scatter + all-gather**；SP 的布局转换正是把这两半拆开（见 §1.3）。
- **reduce-scatter**：先做全局规约（如求和），再只把规约结果的**一片分片**发给每个节点——与 all-reduce「把完整结果发给所有节点」的区别；ZeRO 梯度分片的通信原语（见 [10](./10_训练基础.md) §3）。
- **all-gather**：每个节点中的某向量**拼接起来，传回去**，让每个 GPU 拿到完整向量。
- **barrier（屏障）**：一个「必须等大家都到齐才能继续」的同步点。
- **粗粒度（通信）**：一次通信后做很多计算（与「细粒度、频繁通信」相对）。

---

## 7. 通信相关的环境/执行要点

- **`CUDA_DEVICE_MAX_CONNECTIONS=1`**：强制 compute 与 communication 按确定顺序串行，保证 pipeline parallelism 正确性（Megatron-LM 训练经典优化，详见 [01](./01_部署与环境搭建.md)）。
- **stream 与多 GPU**：一个 CUDA stream 不能跨 GPU，见 [05](./05_GPU硬件与编程基础.md) 的答疑。
