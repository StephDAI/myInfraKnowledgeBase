# 10 训练基础

> 来源：`Diary/2026-09-03.md`、`Diary/2026-09-06.md`（9.3 / 9.6 的训练侧补充）
> 关联：[03 模型与算法](./03_模型与算法.md)（FP4 等低精度量化、LoRA）、[06 分布式并行](./06_分布式并行.md)（DP、集合通信原语）

---

## 1. 混合精度训练：FP16 计算 + FP32 主参数（master weights）

**为什么需要两套参数副本？**

- **FP16（16-bit）参数**：用于实际的神经网络计算（矩阵乘法等），速度更快、显存更省；
- 但 FP16 **数值范围小**，直接以 FP16 做累加式的梯度更新，容易**精度不足**，梯度本身也容易**下溢（underflow）**；
- 因此**优化器内部**额外维护一份高精度的 **FP32「主参数」副本**，更新以 FP32 精度进行。

**一轮训练中参数的流转：**

| 阶段 | 精度 | 说明 |
|---|---|---|
| 前向 / 反向传播 | FP16 参数 | 产出 FP16 梯度 |
| 优化器更新 | FP16 梯度 → 转成 FP32 | 以 FP32 精度更新 FP32 主参数；Adam 的 Moment / Variance 也维护在 FP32 |
| 下一轮前向之前 | FP32 主参数 → 转回 FP16 | 重新生成供下一轮前向传播使用的 FP16 参数 |

```
前向/反向传播：用 FP16 参数  ──►  得到 FP16 梯度
                                    │
优化器更新：FP16 梯度 → 转 FP32 → 更新 FP32 主参数 → 再转回 FP16
                                    ▲
                     Adam Moment / Variance 也是 FP32
```

一句话：**计算用 FP16，更新走 FP32**——低精度负责快和省，高精度负责稳。

---

## 2. 参数、梯度、优化器状态的更新关系（Adam 为例）

参数更新不是「梯度直接改参数」，而是要经过优化器状态中转：

**反向传播算出梯度 → 梯度与上一步的优化器状态一起传入优化器 → 先更新该步的优化器状态 → 再用（更新后的）状态去更新参数。**

```
反向传播算出梯度（g）
      │   g + 上一步优化器状态（m、v）
      ▼
   优化器：更新该步的优化器状态
      │
      ▼
   更新参数（对应 §1：实际以 FP32 主参数形式更新）
```

要点：

- **状态更新在前、参数更新在后**，二者呈顺序依赖，不能并行；
- 优化器状态（Adam 的 m / v，加上 §1 的 FP32 主参数）都是 **FP32**，并且随训练轮次逐轮维护——这是训练显存里除参数、梯度外的另一大块开销，也是 §3 ZeRO 要分片的对象。

---

## 3. DP 下的 ZeRO：把梯度 / 优化器状态 / 参数分片

> 前置：DP（数据并行）= 每张 GPU 持完整模型、跑不同的数据批次（定义见 [06 §1](./06_分布式并行.md)）。朴素 DP 下每张卡都冗余维护一份完整的参数 + 优化器状态，显存随卡数线性膨胀，冗余严重。

**ZeRO（Zero Redundancy Optimizer）的思路**：在 DP 中消除这份**冗余**——每张 GPU 只负责维护梯度 / 优化器状态 / 参数中的**一份分片**，需要完整参数时再通过集合通信组装回来。

由于 **DP 下每张 GPU 跑反向传播的数据不一样**，流程为：

```
① 每张 GPU 先计算「全部参数」相对于自己这批数据的局部梯度
② ReduceScatter：跨卡规约（求和）后分发 → 每张 GPU 只留下自己负责的全局梯度分片
③ 用该梯度分片更新对应的优化器状态分片
④ 更新对应的参数分片
⑤ AllGather：把各卡更新后的参数分片组合 → 每张 GPU 重新获得完整的最新参数
```

对应图解：

```
每张 GPU：用自己的数据反向 → 局部梯度（各卡不同）
      │   ReduceScatter（全局求和后按 rank 分片）
      ▼
每张 GPU 只留下自己负责的全局梯度分片
      │   更新
      ▼
对应的优化器状态分片（FP32 m/v、主参数，见 §1/§2）
      │   更新
      ▼
对应的参数分片
      │   AllGather（拼接广播）
      ▼
每张 GPU 获得完整的最新参数 → 进入下一轮
```

> 术语（原语定义见 [06 §6](./06_分布式并行.md)）：**ReduceScatter** = 先做全局规约（如求和），再只把规约结果的**一片**发给每个节点——与 all-reduce「把规约后的完整结果发给每个节点」的区别就在于此；**AllGather** = 各节点把分片**拼接**起来广播，让每张卡拿到完整向量。

---

## 4. 与知识库其它主题的关系

- 与 [03 §1.1](./03_模型与算法.md) 的 **FP4 存储 / FP8-BF16 计算**是「精度分离」思路的两端：那边是**推理**时把权重量化存储、计算前解量化；这边是**训练**时把 FP32 主参数与优化器状态留在高精度、计算用 FP16。共同点：**不同的精度用于不同的目的，靠「转回/转出」衔接**。
- ZeRO 涉及的 ReduceScatter / AllGather 是 [06 §6](./06_分布式并行.md) 集合通信原语在训练侧的直接用例。
