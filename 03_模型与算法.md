# 03 模型与算法

> 来源：`手记.md` 6.10、7.6–7.8、7.15、7.17、7.19–7.20、7.28、8.2
> 关联：[02 推理框架与引擎](./02_推理框架与引擎.md)、[06 分布式并行](./06_分布式并行.md)

---

## 1. DeepSeek V4（FP4 MoE）

### 1.1 FP4 量化与 H20 的硬件阻塞

- DS V4 是 **FP4 MoE** 模型；原生的 **FP4 MoE kernel 不支持 H20 的 Hopper 架构**（硬件级阻塞，6.10）。
- 替代方案：**HBM 里始终存 FP4（~800GB），kernel 在做矩阵乘前，逐 tile 把 FP4 解到 FP8/BF16 放进片上 SMEM/寄存器，算完即弃**。
- 在 vLLM 里这条路有现成实现：**Marlin FP4 weight-only fallback**（权重量化，激活保持高精度）。
- 复盘结论：MoE 专家层的**量化 GEMM（Marlin wna16）占 GPU 时间 42%**，是头号单一消耗（详见 [09](./09_调优案例复盘.md)）。

### 1.2 DeepSeek MTP（多 token 预测）

**MTP（Multi-Token Prediction）**：对于第 k 个预测头，根据 **第 k-1 个 hidden state 和第 k 个 embedding** 预测第 k 个 hidden state，从而预测第 **k+1** 个 token。**使用主模型自身预测**（不是独立小模型）。

---

## 2. GLM 5.1 / 5.2

- 主任务：**GLM5.1 在摩尔线程（S5000）上的优化**（最终目标：在 S5000 上部署 GLM 5.1 & 5.2 并优化）。
- 可用功能（7.15）：
  1. **PD 分离 + 分层稀疏注意力** → 提高解码并发；
  2. **投机解码** → 可自适应选择草稿模型步数。

### 2.1 PD 分离（Prefill/Decode Disaggregation）

**P/D 节点都可以做完整推理，但各自对 P/D 做优化**；最终推理时，P 节点做好 prefill 再发到 D 节点。

- P 节点：优化 prefill（长上下文、高吞吐计算）。
- D 节点：优化 decode（低延迟、投机解码）。
- 记录中 decode 的每步分三步：`recv_requests → get_next_batch_to_run → run_batch`；`run_batch` 又分 `draft → verify → forward_draft_extend_after_decode`（7.17）。

### 2.2 head / LM Head

- **head**：通常指接在主干网络后面的**任务输出模块**，和 attention head 无关。
- **LM Head**：logits 阶段，把某 token 的 hidden state **投影到词表**（vocab）。

---

## 3. 投机解码（Speculative Decoding）

### 3.1 传统投机解码

小模型自回归 decode 出 k 个 token → 大模型把这 k 个 token **一起做一次前向传播** → 根据概率分布**接受/拒绝**：第一个被拒绝的 token 重新采样，之后的 token 全部作废。

### 3.2 投机解码谱系

| 方法 | 机制 | 关键点 |
|---|---|---|
| **EAGLE** | 用**轻量草稿模型**，输入和 MTP 一样（用 k-1 hidden + k embedding 预测下一 token） | 草稿是独立小模型 |
| **DeepSeek MTP** | 用**主模型**的第 k 个预测头做同样的多步预测 | 草稿不独立，复用主模型 |
| **P-EAGLE** | 对未来位置的 emb & hidden state 用**可学习 mask 占位**，加上位置信息一起送进 transformer layer 预测 | 第一个预测 token 是 Next-Token Prediction，之后是 Multi-Token Prediction |
| **DFlash** | 把上下文 hidden state 转成 KV Cache 喂进草稿模型，再用 mask token 通过 **block diffusion** 批量输出 token | 块内并行出 token |
| **DSpark** | 在 DFlash 基础上加**轻量自回归修正头**（增强块内 token 顺序依赖）+ **置信度头**（筛掉低置信度 token，减少无效验证） | DFlash 的改进 |
| **并行投机解码** | draft 阶段**并行**生成多个 token（不再逐 token 串行） | 8.2 引入 |

### 3.3 `--speculative-eagle-topk`

每步候选 token 数：会生成 **k 叉树的 draft**，利用 **masked attention 做一次 forward verify**，从上到下**剪去该层概率不是最高的枝**；每轮验证的 draft token 总数可通过参数设置。

### 3.4 draft / verify / extend 各自做了什么（❓答疑）

> 记录 7.16、7.17、7.20 关于投机解码流程的问题。

**❓ 投机解码 draft、verify 做了什么？draft model 为何 verify 后还要再跑一遍 extend？**

**💡 解答**（对照 run_batch 三步）：
1. **draft**：草稿模型（EAGLE/MTP）基于当前上下文预测出候选 token（可能一次多个/并行）。
2. **verify**：target 大模型把 draft 出的 token 一起做一次前向，按概率分布**接受/拒绝**（第一个被拒的 token 重采样，其后全作废）。
3. **forward_draft_extend_after_decode（extend）**：verify 之后**再跑一遍 extend**，目的是——
   - 让 **draft model 的 KV cache 与 target model 同步**；
   - 把**验证成功的 token 当时「猜」的 hidden state，改成 target model 真正算出来的**。

**❓ forward_draft_extend 的阻塞原因？**

**💡 解答**（记录 7.16 自答）：为这次 draft 前向组装 `ForwardBatch` 对象——把请求的输入元数据（`input_ids`、`positions`、`seq_lens`、`out_cache_loc`、extend 的起止位置等）通过 `.to(device)` 从主机搬到 GPU；**阻塞是因为需要先等前一步 GPU 前向完成**（`.to(device)` 是同步操作，且依赖上一步结果）。

> 更深一层的「空泡」归因（host 阻塞 pageable 拷贝、单流提交饥饿）见 [09 调优案例复盘](./09_调优案例复盘.md) 案例 3 与工作区 `decode空泡归因与解决方案.md`。

---

## 4. NSA（原生稀疏注意力）

**NSA = Native Sparse Attention，原生稀疏注意力**：通过稀疏化注意力模式降低长上下文注意力成本（分层稀疏注意力是 GLM5.1 提解码并发的功能之一）。

---

## 5. 其它模型相关技术

### 5.1 LoRA（低秩微调）

不修改大模型全部参数，而是在部分层加**低秩矩阵**，训练时只更新这些小矩阵。好处：微调成本低、显存小、训练快；**一个基础模型可搭配多个 LoRA**（各对应一种能力/风格/领域知识）。

### 5.2 Mooncake（分布式 KV cache 存储）

把 KV cache 从单卡 HBM「解耦」出来，扩展到 **HBM、CPU DRAM、SSD，甚至跨机器网络内存**，从而支撑更长上下文、更高吞吐、更低成本推理。
