# 04 性能分析方法论

> 来源：`手记.md` 6.11、6.12、6.17、6.22、6.24、7.13、7.15、8.17
> 关联：[02 推理框架与引擎](./02_推理框架与引擎.md)、[09 调优案例复盘](./09_调优案例复盘.md)

---

## 1. 核心指标

- **TTFT**（Time To First Token）：请求发出到第一个 token 产生的延迟。
- **TPOT**（Time Per Output Token）：首 token 之后，每个后续 token 的平均间隔。
- 单请求看 TTFT、TPOT；压测看聚合吞吐（tok/s）。

---

## 2. profiling 手段清单

| 手段 | 作用 |
|---|---|
| `vllm bench serve` | 压测：给定并发/输入输出长度，测吞吐、TTFT、TPOT |
| `vllm` 的 `/metrics`（Prometheus） | 拉取 `request_{queue,prefill,decode}_time_seconds_{sum,count}` 等细粒度指标 |
| `nvidia-smi dmon` | 抓 GPU 利用率（SM、显存带宽），看负载下 GPU 到底打没打满 |
| `nsys`（Nsight Systems） | 系统级/GPU 级 timeline 采集 |
| NVTX | 在代码里打标记，让 timeline 上出现可读的阶段边界 |
| torch 自带 profiler | 逐 kernel 统计，且不禁用 CUDA graph 也能无偏差采集（见 §3） |
| `--cuda-graph-trace=node` | 把 CUDA graph 展开成单个 kernel（否则整个 decode graph 录成一坨） |
| GPU timeline / GPU Kernel Summary / 单 kernel 耗时 | nsys GUI 里的视图，用于定位热点 kernel |

---

## 3. 如何收集准确、详细、方便阅读的 profiling 数据（❓答疑）

> 记录 6.17 的问题。

**💡 解答**（方法论，来自记录 6.22–6.24 + 复盘文档）：

1. **分层自顶向下**，不要一上来就采 kernel：
   - ① 黑盒延迟（`bench serve` 并发扫描）→ ② 资源层（`nvidia-smi dmon` 看 SM/带宽谁打满）→ ③ 延迟分解（`/metrics` 的 queue/prefill/decode 分段）→ ④ kernel 级（`nsys`）。
2. **用 nsys 的正确姿势**（复盘文档给的命令）：
   - 「武装启动」挂探针但不采集：`nsys launch --session-new=vllmprof -t cuda,nvtx --cuda-graph-trace=node vllm serve ...`；
   - 服务就绪后 `nsys start` → 跑极小压测 → `nsys stop`；
   - 导出：`nsys stats --report cuda_gpu_kern_sum --format csv ... > kern_sum.csv`。
   - 关键 flags：`--cuda-graph-trace=node` 展开 graph；`-s none` 关 CPU 采样降开销。
3. **NVTX 打点**：在关键阶段（`recv_requests` / `get_next_batch_to_run` / `run_batch` 等）打标记，timeline 才可读。
4. **逐 kernel 统计而不禁用 graph**：用 **torch 自带 profiling 工具**（它能在 graph 内逐 kernel 记账，且不引入禁用 graph 带来的偏差）。`enforce-eager` 只在「要看单 kernel 边界/gap」时才开，代价是 `cudaLaunchKernel` 开销变大。
5. **必须 warmup**：冷启动的一次性开销（CUDA graph 捕获新 shape、kernel autotune）会污染均值——复盘里 prefill 扫描的非单调噪声即源于此。
6. **采集窗口要小、可对比**：固定 workload，同一套流程跑 baseline 与优化后各一遍，才能比出差异。

**❓ `start_profile` 请求是什么？step 是什么？如何看 timeline？**（7.15）

**💡 解答**：
- `start_profile`：控制 profile 采集起止的 HTTP 请求（开始采集 → 压测 → 停止采集）。
- **step**：一次前向推理——**一次 prefill 或一次 decode 都算一个 step**。
- 看 timeline：用 **Nsight Systems GUI**（`.nsys-rep`）或把 trace 转成 json 后用 chrome://tracing / Perfetto 打开，看 GPU timeline、Kernel Summary、单 kernel 耗时。

---

## 4. compute-bound vs memory-bound（关键判定）

**判定方法**：负载下同时看 SM 利用率和显存带宽利用率。

- 实测（c=32）：**SM（计算单元）利用率中位 100%、98% 采样 ≥90% → 计算单元几乎打满**；**显存带宽利用率均值仅 23%、峰值 30% → 带宽远没吃满**。
- → 判定：这种输入/输出形状下，服务是 **compute-bound（算力瓶颈）**，不是显存带宽瓶颈。

**为什么这个区分极重要**——它决定优化方向：
- **算力瓶颈** → 靠量化 / 更优 kernel / MoE 调度（EP、负载均衡）；
- **带宽瓶颈** → 靠减小 KV / 批处理（KV cache 优化、量化 KV 等）。

---

## 5. 并发扫描曲线（sweep）

补全并发扫描，看吞吐和延迟随并发怎么变、拐点在哪。已有 c=1（单流 85 tok/s）和 c=32，再补 c=8、c=64：

| 并发 | 总吞吐 tok/s | TTFT | TPOT | 每流 tok/s |
| ---- | ------------ | ---- | ---- | ---------- |
| 1    | （单流）     | 1.1s | 12ms | 85         |
| 8    | 1542         | 1.7s | 20ms | 38         |
| 32   | 2406         | 4.1s | 51ms | 15         |
| 64   | 3162         | 6.0s | 78ms | 10         |

**读法**：聚合吞吐还在涨（未到平台期），但每流速度、TTFT、TPOT 全在恶化，**TTFT 涨得最凶**（交互式体验第一约束）。

---

## 6. 延迟分解与隔离测量

**TTFT 分解**：c=32 下 `TTFT = queue（排队）+ prefill（首 token 前向）`。实测 queue 0.9s / prefill 2.9s。

**c=1 隔离测量**：在并发=1（无竞争）下测纯 prefill ≈ 0.2s，而负载下 prefill 2.9s → **~14× 是竞争造成，不是 prefill kernel 本身贵**。

→ 修正诊断：根因是**算力饱和下 prefill 与 decode 的调度竞争**，而非 prefill kernel 本身慢。

---

## 7. 屋顶模型（Roofline）

**算术强度（Arithmetic Intensity, AI）**：每从内存读 1 字节数据，程序可执行多少浮点运算，单位 **FLOP/Byte**。

- **脊点左侧**：AI↑，FLOP/s↑ → **内存瓶颈**；
- **脊点右侧**：AI↑，性能不变 → **计算瓶颈**。

---

## 8. 其它测量手段

### 8.1 wall clock（墙钟）

从进程外部用真实时钟（`time.perf_counter()`、`gettimeofday` 等）测量的端到端流逝时间。GPU 场景测「这轮操作真实耗时」的正确姿势：

```python
torch.musa.synchronize()          # 先等 GPU 把之前的活全干完
t0 = time.perf_counter()          # 记下当前时间
buffer.low_latency_dispatch(...)  # 干活
buffer.low_latency_combine(...)
torch.musa.synchronize()          # 等 GPU 把这轮活干完
t1 = time.perf_counter()          # 再记下时间
# t1 - t0 = 这轮操作的真实耗时
```

### 8.2 A/B 测试

将用户或样本随机分成两组（或多组），分别暴露于不同版本（A 版 vs B 版），通过对比关键指标判断哪个版本更优。
