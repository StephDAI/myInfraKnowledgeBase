# 02 推理框架与引擎

> 来源：`手记.md` 6.10–6.11、6.17、6.30、7.6–7.8、7.12、7.15、7.19、8.30；`read_note.md`（TVM 深入）
> 关联：[03 模型与算法](./03_模型与算法.md)、[04 性能分析方法论](./04_性能分析方法论.md)、[09 调优案例复盘](./09_调优案例复盘.md)

---

## 1. 框架全景速览

| 框架/工具 | 定位 | 一句话 |
|---|---|---|
| **vLLM** | 高吞吐 LLM 推理引擎 | 重点做 continuous batching、CUDA graph、PagedAttention/KV cache 管理 |
| **SGLang** | LLM 推理引擎 | 用 RadixAttention 做前缀共享，适合 agent/RAG/工具调用等多步骤程序 |
| **TensorRT** | NVIDIA 通用推理优化引擎 | 聚焦模型底层优化：Builder 优化生成 Engine → Runtime 加载执行 |
| **ONNX** | 深度学习模型通用中间格式 | 描述模型计算图，便于框架间迁移：训练框架 A → ONNX → 推理框架 B |
| **ONNX Runtime** | ONNX 模型的跨平台推理引擎 | 让 ONNX 模型在不同平台/硬件上运行 |
| **TVM** | 编译器 | 图级优化 + 算子级优化 + 面向硬件的代码生成 |

---

## 2. vLLM

### 2.1 核心加速内核/机制

- **DeepGEMM**：加速矩阵乘法的 kernel 库。
- **CUDA graph capture**：把一次 forward 的一串 kernel 捕获成一张图，之后只需一次 launch 重放，消除 CPU launch 开销。
- **autotune**：对同一个计算，在候选 kernel 实现里选当前配置下最快的内核。
- **FlashInfer**：加速推理的 kernel 库，功能更全面（含注意力、量化 GEMM、ragged 等）。

> 这些与 [04 性能分析方法论](./04_性能分析方法论.md) 里的 profiling 手段（`/metrics`、`bench serve`、`nvidia-smi dmon`）配合使用。

### 2.2 显存组成（gpu-memory-util）

`gpu-memory-util` 包含：**权重 + KV Cache 池 + CUDA graph 显存 + …**

- 权重：模型参数（量化后显著缩小）。
- KV Cache 池：按 `--max-model-len` 和 `--gpu-memory-utilization` 预分配。
- CUDA graph 显存：为每个固定 batch size 捕获的 graph 也要占显存（见 2.5）。

### 2.3 prefix caching（前缀缓存）

**共享前缀缓存**：用 LRU 管理 KV cache block；下一轮请求的前缀若对得上，就**直接复用**已有的 KV cache block，避免重复 prefill 计算。

- 只对「前缀逐 token 完全一致」的请求有效。
- 在真实流量中命中率取决于请求前缀重合度（案例中压测观察到 0 命中，见 [09](./09_调优案例复盘.md)）。

### 2.4 chunked prefill + continuous batching

**continuous batching（cont. batching）**：新请求的 prefill 可以和旧请求的 decode **一起做 forward pass**——请求之间靠 **attention mask + KV cache 隔离**保证互不影响。

**chunked prefill**：把很长的 prefill 切成多个 chunk，和 decode 交错调度，避免一个大 prefill 独占 GPU、饿死已在 decode 的请求。

**batch（批）**：一次送进 GPU 处理多个请求。

batch=4 做 decode 的例子（6.30）：4 个 token 放在一起作为 input → 得到完整 `4×7168` hidden state → 拼出 `4×vocab` 的 logits → 4 行中各采样 1 个 token。

#### chunked prefill 四连问（❓答疑）

> 记录 6.30 的一组问题。

**❓ prefill 还没结束，怎么做到已经开始 decode 了？**

**💡 解答**：chunked prefill 把长 prefill **切成多个 chunk**，每算完一个 chunk，调度器就可以在 chunk 之间**插入其他请求的 decode step**。所以「这个请求的 prefill 还没全部完成」时，引擎已经在跑（同一批里其他请求的）decode 了——prefill 与 decode 交错调度，这正是 cont. batching 的体现。

**❓ 为什么 prefill 期间的 decode graph 只要一次 allReduce？**

**💡 解答**：decode 走的是**预捕获的 CUDA graph**，graph 里 kernel 已经融合/合并，通信的 all-reduce 被 fuse 成更少的显式通信（如 fused allreduce / attention-output allreduce 合并进下一个 kernel）；而 prefill 是 **eager 逐 kernel launch**，每个需要通信的点单独 launch，所以 timeline 上看到的通信次数更多。具体以实际 timeline 为准。

**❓ 理论上 prefill 不就是大一点的 decode 吗？为什么 prefill 相关 kernel 跟 decode 不一样？**

**💡 解答**：结构都是 attention + MoE，但两者差别很大：
- **decode**：每请求 1 个 token、seq 长度固定（静态形状）→ 用静态 CUDA graph；attention 是**增量**（只算新 token 对已有 KV 的注意力，O(N)）。
- **prefill**：变长大 seq（如 1024/3073），形状动态 → 用不了静态 graph，走 eager / PIECEWISE；attention 是**一次性算完整 KV**（O(N²)）。
- 所以二者的 kernel 实现、形状、launch 方式都不同。

**❓ prefill 阶段的 MoE 在哪里？**

**💡 解答**：**和 decode 的 MoE 混在一起**——cont. batching 下同一 forward 里既有 prefill chunk 又有 decode token，MoE 专家层对二者统一处理（prefill token 与 decode token 都进同一个 all-to-all + 专家 GEMM），timeline 上二者的 MoE 无法简单切开。

---

### 2.5 CUDA graph：enforce-eager vs capture，FULL vs PIECEWISE

**enforce-eager 模式**：主动关闭 CUDA Graph，让每个 kernel 单独 launch、在 timeline 上独立可见。
- 好处：能看清单个 kernel、算子边界、kernel 间 gap。
- 代价：`cudaLaunchKernel` 开销增大（CPU 逐个 launch）。

**启用 CUDA Graph**：kernel 被合并成图重放，CPU 开销小，但单 kernel 不可见（除非用 `--cuda-graph-trace=node` 展开）。

**FULL graph vs PIECEWISE graph**：
- **FULL graph**：形状固定的 decode 路径（bs 固定）预编译成完整图，decode token 走 FULL graph。
- **PIECEWISE graph**：prefill 是动态/大 seq 形状，用不了静态 FULL graph，走独立的 PIECEWISE graph（或纯 eager）。

**`--cuda-graph-max-bs-decode`**：控制 decode 生成阶段 CUDA Graph 优化能覆盖到多大的 batch size。
- 框架通常为**若干个固定 batch size 分别捕获 graph**，batch size 越大，显存占用越大。

> **为什么 W3 prefill 显示 1024 而不是 1024+3？**（6.30）——因为那 3 个 decode token 走了**预编译的 FULL graph**，而 prefill 走独立的 **PIECEWISE graph**，timeline 上 prefill 只显示自己的 1024 token（可用 timeline 线索验证）。

### 2.6 decode step 的 kernel 构成（❓答疑）

> 记录 6.29 的一组问题，集中解答。

**❓ logits_processor 是什么？53 个非 graph kernel 是哪些？kv cache 更新的 kernel 有哪些？**

**💡 解答**：
- **logits processor**：decode 的「尾巴」——把最后一层 hidden state 经 **LM Head 投影到词表得到 logits**，再做采样/温度/logit 后处理（详见 [03](./03_模型与算法.md) 的 head/LM Head）。它通常不在 decode 的 CUDA graph 里，而是作为**非 graph kernel** 逐 step 跑。
- 一个 decode step 的 kernel 组成 = **graph 重放 + 非 graph kernel**。非 graph kernel 大致三类：
  1. **logits processor**（logits 投影 + 采样）；
  2. **NCCL all-gather**（TP 下把各 rank 的局部 logits/采样结果汇聚）；
  3. **KV cache 更新相关 kernel**（把本步新 token 的 K/V 写进 cache block）。
- 「53 个非 graph kernel」是某次 timeline 中 device 上观察到的具体个数，需要在 GUI 里把「CUDA graph 之外」的 kernel 单独筛出来看，落在每个 decode step 的 graph 结束之后、下一个 step 之前。

**❓ 什么叫 Step N 时 CPU 先 launch graph，再进入 logits_processor？logits_processor 本身不是 graph 吗？graph 1694 是一堆 kernel 的集合吗？**

**💡 解答**：
- 一个 step 的顺序：CPU 先 `launch`（重放）decode 的 CUDA graph（算到最后一层 hidden state）→ 之后 CPU 再进入 logits_processor（在 graph 之外单独 launch 的 kernel）。
- **logits_processor 一般不是 graph**（至少不是主 decode graph 的一部分），因为它的形状依赖采样结果、且常被排除在 capture 之外。
- **graph 1694**：就是一张被捕获的 CUDA graph 的编号，内部确实**是一堆 kernel 的集合**（重放时一次 launch 依次执行）。在 GUI 里可以用 `--cuda-graph-trace=node` 把它展开成单个 kernel 来看（[04](./04_性能分析方法论.md)）。

### 2.7 Flash-Decoding

通过 **KV 维度的切块（Split-K）**——把所有 token cache 分 chunk、一个 SM 负责一组 token——提升了 **decode 阶段**的 SM 利用率（decode 时 query 少、KV 长，按 KV 切块能让更多 SM 参与）。

> 与之区分：**SP（Sequence Parallelism）**解决的是「单卡显存塞不下超长上下文」，一个 GPU 只存一部分 token 的 cache（见 [06](./06_分布式并行.md)）。

### 2.8 FlashInfer 的 ragged QO

将不同请求中（属于同一 batch）**数量不等的 Query Token 紧密拼接**，并用 `qo_indptr` 标记每个请求的 Q/O 边界（8.30）：

```
Q         = [P(96), S0(4), S1(4)]   # 总共 104 个 token
qo_indptr = [0, 100, 104]           # 两个 request 的边界
```

好处：避免对不同长度 query 做 padding 浪费算力，稀疏/不等长的 batch 也能高效算注意力。

---

## 3. SGLang

### 3.1 定位

适用 **agent、RAG、工具调用**等多步骤程序——这些场景有大量共享前缀的生成，SGLang 的前缀复用能力收益最大。

### 3.2 generation primitive + RadixAttention（❓答疑）

**❓ RadixAttention 是什么？prefix graph execution problem 是什么？**

**💡 解答**：
- **generation primitive**：SGLang 里对话的「基本生成单元」（如一次 `gen`、`select`、`extend` 等）。一个对话由多个 primitive 构成，即 **execution graph**。
- **RadixAttention**：用 **Radix 树（前缀树）** 组织 KV cache。树按 token 前缀组织，多个 primitive/请求只要前缀一致，就落到树的同一路径上、**共享同一份 KV cache**，避免重复 prefill。
- 记录里 "RadixAttention: prefix?" 的疑问即此——**RadixAttention 的本质就是「按 token 级 prefix 匹配来共享 KV cache」**。
- **prefix graph execution problem**：指「在由多个 primitive 构成的分支/合并执行图上，如何做前缀共享与调度」的问题——即哪些节点能共享前缀、何时该 reuse 何时该 copy-on-write，是 Radix 缓存管理的核心难点。

### 3.3 PD 分离、投机解码相关参数

见 [03 模型与算法](./03_模型与算法.md)（PD 分离、EAGLE/MTP、`--speculative-eagle-topk`、`--cuda-graph-max-bs-decode`）。

---

## 4. TensorRT / ONNX / ONNX Runtime

- **TensorRT**：Builder 优化并生成 Engine → Runtime 加载 Engine 执行推理。**保持原模型推理语义，只优化内部计算图和执行方式**。
- **ONNX**：训练框架 A（PyTorch/TensorFlow…）→ ONNX → 推理框架 B 的**通用中间格式**，本质是描述模型的**计算图**。
- **ONNX Runtime**：功能大体与 TensorRT 类似，但面向**多平台多硬件**运行 ONNX 模型。

---

## 5. TVM

> 来源：`手记.md` 7.11–7.12 + `read_note.md`

TVM 是一种**编译器**，**给深度学习模型提供跨硬件的端到端自动优化编译能力**，同时做三件事：
1. **graph-level optimization**：计算图级优化；
2. **operator-level optimization**：算子级优化；
3. **hardware-specific code generation**：针对不同硬件生成低层代码。

### 5.1 三个模块

1. **Tensor expression language + schedule primitives**——「计算什么」与「怎么优化地算」；
2. **Automated program optimization framework**——基于 ML 自动搜索高性能 schedule；
3. **Graph rewriter**——高层图优化。

### 5.2 图级优化

图级优化的四类手段：
1. **算子融合**（operator fusion，自动生成算子实现）
2. **constant folding**（常量折叠）
3. **static memory planning**（静态内存规划）
4. **data layout transformation**（数据布局变换）

### 5.3 生成算子时的优化

#### Tensorization

- **Intrinsic**：编译器认识的特殊硬件操作或优化微内核，如 `gemm8x8`。硬件虽然提供了 `gemm8x8` 之类的能力（即 tensor compute primitive），但**需要把这个硬件 primitive 注册/声明给编译器**。
- **Tensor intrinsic declaration**：同时声明这个操作「数学上算什么」，以及「在目标硬件上如何实现」。
- **匹配流程（lowering rule）**：对于普通张量计算，scheduler 通过 split、reorder、cache、layout transformation 等操作，把循环整理成硬件 intrinsic 所要求的形状；TVM 检查内部循环是否与 intrinsic 模式匹配，匹配成功后，把普通的循环**替换为硬件指令**。

#### Simplifying Latency Hiding by Virtual Threading

把一个单线程 pipeline 程序，逻辑上拆成多个「虚拟线程」，每个虚拟线程处理不同的数据块；然后编译器自动把这些虚拟线程的操作**交错排列**，并**插入同步**，让硬件能够 DAE（decoupled access-execute）流水并行执行。

### 5.4 大 schedule space 下的自动调度优化器

**cost model**：输入一个候选方案，模型预测它大概有多快/多慢，然后用预测结果挑出更有希望的方案。

统计式 ML cost model 的流程：
1. schedule explorer 生成候选 schedule；
2. lowering 成低层 loop program；
3. ML model 预测运行时间（常用 XGBoost）；
4. 选择看起来好的配置在真实硬件上测量——常用**并行模拟退火**选 top-k：从随机配置开始 → 随机移动到邻近配置 → 模型预测更快则接受、更慢则以一定概率接受 → 多个搜索过程并行进行；
5. 把测量结果加入训练集；
6. 更新模型。

> 该模型**不需要用户输入详细硬件参数**；随着测量数据增加，会越来越适配当前硬件。

### 5.5 编译流水线

```text
                   深度学习框架模型
             TensorFlow / MXNet / PyTorch
                           ↓
                     TVM Frontend
                           ↓
                      计算图 IR
                           ↓
         ┌────────────────────────────────┐
         │          图级优化               │
         │  fusion / constant folding      │
         │  memory planning / layout       │
         └────────────────────────────────┘
                           ↓
                    优化后的计算图
                           ↓
         ┌────────────────────────────────┐
         │       Tensor Expression         │
         │   描述每个算子数学计算          │
         └────────────────────────────────┘
                           ↓
         ┌────────────────────────────────┐
         │            Schedule             │
         │ tile / reorder / bind / cache   │
         │ tensorize / virtual thread      │
         └────────────────────────────────┘
                           ↓
         ┌────────────────────────────────┐
         │        自动调优                 │
         │ ML cost model + real measurement│
         └────────────────────────────────┘
                           ↓
         ┌────────────────────────────────┐
         │         代码生成后端             │
         │ LLVM / CUDA / OpenCL / Metal    │
         │ Custom Accelerator Backend      │
         └────────────────────────────────┘
                           ↓
                     可部署模块
```
