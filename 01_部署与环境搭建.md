# 01 部署与环境搭建

> 来源：`手记.md` 6.9–6.17、7.7、7.15、7.22、8.3 相关条目
> 关联：[05 GPU 硬件与编程基础](./05_GPU硬件与编程基础.md)、[06 分布式并行](./06_分布式并行.md)、[09 调优案例复盘](./09_调优案例复盘.md)

本文件汇总实习期间反复遇到的环境与部署问题及解法。

---

## 1. 裸机部署 vs Docker 部署

**裸机部署**：服务器上直接装依赖、直接 `sglang.launch_server`。优点是**更适合运行时追踪与优化**（可以随意改环境、挂 profiler、换源码），缺点是依赖冲突多。

- 6.9 首次尝试裸机部署 SGLang 时遇到 **FlashMLA 依赖问题**（SGLang 依赖 FlashMLA 加速 MLA 注意力，编译/导入是常见卡点）。
- 6.10 用 conda 整理环境后，**编译/导入 FlashMLA 仍然失败**，判断 Docker 部署环境问题更少，转用 Docker。

**Docker 部署**：依赖打包在镜像里，环境一致性好，但不利于深度运行时追踪。

**结论（经验）**：做运行时追踪与优化时优先裸机；图省事/稳定时用 Docker。两者按目标取舍。

---

## 2. CUDA / 驱动环境

### 2.1 核心教训：CUDA 环境不要 pip 装

> **关键教训**：机器上没有系统 CUDA toolkit 时，**不要用 pip 装 CUDA**。

- 6.16 从头部署时踩坑记录（加粗强调）。
- 正确做法：通过 conda（装系统级 CUDA）或系统包管理器安装与驱动匹配的 CUDA toolkit。

### 2.2 CUDA 版本不匹配

- 6.17 启动时出现 **CUDA 版本不匹配** 问题，需要先解决 cuda 环境问题（驱动/运行时/toolkit 三者版本对齐）才能启动。
- 7.7 又因版本问题，**conda 重装系统级 CUDA**。

### 2.3 NVIDIA 驱动安装 & 磁盘扩容

- 6.15 到新机器 8×H20 后装 NVIDIA 驱动，发现**磁盘空间不够**。
- 6.16 **改 RAID0 扩容**后从头部署：开 conda 环境 → 装 NVIDIA 驱动 → 装 vLLM → 拉权重。

### 2.4 启动 OOM

- 6.17 启动时出现 **OOM**。gpu-memory-util 占满，因为显存同时装：**权重 + KV Cache 池 + CUDA graph 显存 + …**（详见 [02 推理框架与引擎](./02_推理框架与引擎.md) 的显存组成）。

### 2.5 模型启动时间过长导致 ssh 断连

- 6.17 采集 profiling 时遇到的问题：**模型启动时间过长，导致 ssh 无法保持连接**。
- 解决思路：用 `nohup`/`screen`/`tmux` 挂后台、或配置 ssh `ServerAliveInterval`，避免长时间无输出被断链。

---

## 3. MUSA 环境（摩尔线程 S5000）

- **MUSA SDK 位置**：`/usr/local/musa`（7.22）。
- **源码/构建工作流**（8.3）：编辑 `.cu` 文件，通过 `.mu` 符号链接让构建系统以 MUSA 模式编译同一份代码 —— 即 **`.cu` 是规范源，`.mu` 是参与 MUSA 编译的同一份代码**（见 [05](./05_GPU硬件与编程基础.md) 与 [08](./08_DeepEP与MoE通信算子.md) 中 combine 优化的"改 `.cu` 后须同步 `.mu`"约定）。

> MUSA 与 CUDA 的完整对应关系见 [07 互连与网络技术](./07_互连与网络技术.md) 的「MUSA vs CUDA」一节。

---

## 4. 环境相关调优点

### 4.1 `CUDA_DEVICE_MAX_CONNECTIONS=1`

Megatron-LM 训练场景的经典优化：**强制 compute 和 communication 按确定顺序串行**，保证 pipeline parallelism 的正确性（6.22）。

- 它限制的是**单个 device 上同时 pending 的 kernel 数量**（compute kernel 与 communication kernel 不能同时排队），从而避免乱序执行破坏流水线语义。
- 推理场景一般不需要，但在调试/复现 pipeline 问题时可能有用。

### 4.2 docker 环境里的脚本与请求

- 7.15 上机后了解 docker 环境、各个脚本含义；通过 HTTP 发 `start_profile` 请求触发采集（`start_profile` 是一次 profile 的起止控制请求，见 [04 性能分析方法论](./04_性能分析方法论.md)）。

---

## 5. 部署过程常见坑速查表

| 坑 | 现象 | 解法 |
|---|---|---|
| FlashMLA 依赖 | SGLang 启动/导入失败 | 换 Docker，或正确编译 FlashMLA |
| pip 装 CUDA | 运行时/驱动/toolkit 不匹配 | conda 装系统级 CUDA |
| 磁盘不足 | 权重/镜像装不下 | RAID0 扩容 |
| 启动 OOM | gpu-memory-util 超限 | 降 `--gpu-memory-utilization`、减 KV cache、调 `--cuda-graph-max-bs` |
| 长时间无输出断 ssh | 采集中断 | `nohup`/`tmux`/`screen` + ssh keepalive |
