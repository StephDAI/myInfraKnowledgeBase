# 09 调优案例复盘

> 来源：`手记.md` 6.11–6.12、6.26、6.29–6.30、7.22、7.24、7.27、8.20
> 参考：工作区 `DeepSeek-V4-Pro vLLM 部署 Profiling 复盘.md`、`GLM5.1-S5000调优记录.md`、`decode空泡归因与解决方案.md`
> 关联：[02 推理框架与引擎](./02_推理框架与引擎.md)、[04 性能分析方法论](./04_性能分析方法论.md)、[08 DeepEP与MoE通信算子](./08_DeepEP与MoE通信算子.md)

---

## 案例 1：H20 部署 DeepSeek V4 的 compute-bound 诊断

**背景**：8×H20，TP=8，vLLM 部署 DeepSeek V4（FP8 量化，MoE 专家层 Marlin wna16）。

**漏斗式排查链**（自顶向下，每步结论排除一类可能并逼出下一个更窄问题）：

1. **并发扫描**（`bench serve` c=1/8/32/64）：加并发后聚合吞吐还在涨（未到平台期），但每流速度、TTFT、TPOT 全恶化，**TTFT 涨得最凶**（1.1s→6.0s）——交互式第一约束是 TTFT。
2. **资源层**（`nvidia-smi dmon`）：SM 中位 100%（98% 采样 ≥90%），显存带宽平均仅 23% → **compute-bound（算力瓶颈）**，不是带宽瓶颈。
3. **延迟分解**（`/metrics`）：TTFT = queue 912ms（21%）+ prefill 2862ms（67%）。
4. **隔离测量**（c=1）：纯 prefill ≈ 200ms vs 负载下 2862ms → **~14× 是竞争造成，不是 prefill kernel 本身贵**。
5. **kernel 级归因**（nsys，`--cuda-graph-trace=node`）：GEMM 真计算 78.3%（MoE 44% + 稠密 25% + attention 9%），**TP 通信仅 10%**。

**最终结论**：**瓶颈 = MoE 专家层量化 GEMM（Marlin wna16，占 42%），TP 通信是次要第二顺位（10%），计算占主导**。诊断演进：算力瓶颈 → prefill 被调度竞争拖慢 → MoE 专家量化 GEMM 主导。

**修正诊断的关键**（6.11）：根因是**算力饱和下 prefill 与 decode 的调度竞争**，而非 prefill kernel 本身慢。后续方向：逐层定位 MoE GEMM、查通信暴露、评估 EP/换 MoE kernel 后端/量化方案、用 `max-num-batched-tokens`/`max-num-seqs` 保护 TTFT。

---

## 案例 2：8×H20 nsys 的 all-reduce skew 归因

**现象**：定制构建没有 torch profiler，改用 nsys 测「饱和的 SM 在算什么」；用 NVTX + enforce-eager 展开看单 kernel，发现占绝大多数时间的 kernel 是 vLLM 实现的一套 **all-reduce kernel**，由此发现 **execution divergence（执行发散）** 问题。

> 教训（6.26）：**不同发请求的方法会得到不一样的 timeline 分析结果**——timeline 结论高度依赖 workload 形状，all-reduce 占比高很可能只是「5 条顺序简单请求」这种低负载模式的表象，换一种请求方式结论就变。做归因前要先确认 workload 的代表性。

### 2.1 两类 workload 的观察

**（a）简单串行低 workload**：all-reduce barrier 在等所有 GPU 完成上浪费大量时间，而且每次都是**某个 GPU 固定拖后腿**（prefill 阶段 TP1、decode 阶段 TP7）。

**（b）8 路并发长请求（3k tok/req）**：TTFT 极高，8 个请求的 prefill 并没有真正并行处理，而是接近串行排队——vLLM 调度器在 chunked-prefill + PIECEWISE CUDA graph 模式下，似乎**没把多请求的 prefill chunk 合并进同一 iteration，而是逐请求串行 prefill**。

### 2.2 根因细化（6.29）

- **主要原因：all-reduce barrier 通信跨设备阻塞**。每张卡真实计算量与耗时基本一样，但**总有某张卡 kernel launch 得慢，导致晚到下一个 barrier，别的卡只能 spin-wait**。
- **长 prompt 请求的 prefill 阶段**：prefill 是动态/大 seq 形状（3073、1024），用不了需静态形状的 CUDA Graph，**每个 kernel 只能由 CPU 一个一个 launch**。timeline 模式：上一次 all-reduce 同步后各卡 eager 地 launch 计算 kernel、**launch 时机不齐** → 这一步 all-reduce 又开始等。**归因：主机侧 eager 执行抖动**。
- **短 prompt 请求的 decode 阶段**：一个 decode step = **graph 重放 + 非 graph kernel**（logits processor、NCCL all-gather、KV cache 更新）。graph 重放结束时间一致，但 **device 7 上非 graph kernel 执行很慢**，导致 device 7 还在执行上一 step 的非 graph kernel 时，别的 GPU 已走到下一 step 的 custom all-reduce kernel（该 kernel 需所有 GPU 完成才能继续）→ 大量等待。
- **device 7 延迟来自多段 CPU 空洞**，最大空洞原因是 **inter-rank 调度器同步延迟（待研究）**。

### 2.3 优化点汇总

**来自简单串行低 workload**：
1. **all-reduce barrier skew**（TP1/TP7 拖后腿）——方向：排查某 GPU 为什么总是慢（为什么产生 skew）、减少等待：
   - 消除 **CPU launch skew**：针对 prefill 把 62 个小 graph 合成一个大 graph，一次 launch、所有 device 同步启动；
   - 非 graph kernel（KV cache 操作）也纳入 graph，消除层间不确定延迟。
2. （可选、幅度小）**fuse tiny kernels**：如 `scale_1x128_kernel` 每次都紧跟 `fp8_gemm_kernel_swapAB` 后面。

**来自 8 路并发长请求**：chunked-prefill 未合并多请求 prefill chunk、逐请求串行 → 需要调度器把多请求 prefill chunk 合并进同一 iteration。

---

## 案例 3：S5000 部署 GLM5.1 的通信算子优化

**最终目标**：在 S5000 上部署 GLM 5.1 & 5.2 并优化（7.24）。

**路径**：
1. **前期**：SGLang 参数调优（构建调参知识库，835 参数 → 722 性能相关），**TTFT ↓20%**；TPOT 基本无改进。
2. **Decode 节点 profiling**：发现**通信算子为瓶颈**（GPU-busy 里 DeepEP all-to-all 占 33.4%，完全暴露、零重叠）；进一步定位到**瓶颈在 dispatch 的 P2P send 阶段**（8.18）。
3. **目前**：优化通信算子。

**困难与已有**：
- 困难：**没有官方算子源码 MT-DeepEP**（多机通信闭源）。
- 已有：① CUDA 版源码；② MUSA 官方编译版（可分析接口/调用库；多节点通信、单节点卡间全用 RDMA）；③ 早期开源版本（只有未优化的单节点通信算子）。

**现状（7.27）**：通信采用「同节点 **IPC P2P** + 跨节点 **NVSHMEM RMA**（但没用 IGBDA 实现，仍需 CPU 提交 RDMA）」→ **需要改成全部 IGBDA，与 CUDA 版一致**。

### 3.1 SBO（single-batch overlap）卡点

> 记录 7.22/7.24 + `GLM5.1-S5000调优记录.md`。

SBO 是「生产者-消费者 signal 协议」：专家 down-GEMM 只用一部分 SM，算完每个 M-block 往 `signal` 张量写完成标记；DeepEP `low_latency_combine` 读 signal，**GEMM 出一块就发一块**，让 combine 通信与 down-GEMM 计算重叠。

**要素三方配合**，当前卡点：
1. **mate（MoE GEMM）**：signal 逐块写出已实现，但 masked 路径**对外接口没暴露 `num_mp`/`num_sms`**（无法把 down-GEMM 限制到 N 个 SM）。
2. **DeepEP combine**：MUSA 二进制**无 overlap 通路（闭源）**——`buffer.py` 的 `low_latency_combine` **没有 `overlap / comp_signal / num_sms / block_m / threshold` 参数**，`.so` 符号里也没有 comp_signal/overlap。
3. **框架（SGLang）**：把两者用 CUDA event/alt-stream 串起来并按平台开关。

→ 这是 decode 优化的**最大硬骨头**：需自己实现支持 SBO 的通信算子（多机通信 DeepEP 源码未开源）。

### 3.2 decode 空泡（衔接）

decode 每步两类 ~10ms GPU 空泡（阻塞 pageable H2D 拷贝 / 单流提交饥饿）与 DeepEP 通信暴露，详见工作区 `decode空泡归因与解决方案.md`——其机制基础（launch≠execute、flush）见 [05](./05_GPU硬件与编程基础.md) 第 4 节。

---

## 案例 4：mpirun 独立进程 vs spawn 的 EAGAIN（❓答疑）

> 记录 8.20：test internode 用 `mpirun -np 16 -host node1:8,node2:8` 启动，每节点 8 个独立进程，每个 `set_device(local_rank)` 后创建 `deep_ep.Buffer()`，结果为 **EAGAIN**。

**❓ 这一操作是否合理？sglang 单进程 + `torch.multiprocessing.spawn` 与 mpirun 拉 8 个独立进程的差异？**

**💡 解答**：

- **EAGAIN = "resource temporarily unavailable"**，典型来源是初始化阶段**并发抢占某类有限资源**：IB 网卡的 QP（queue pair）数量、NVSHMEM bootstrap/unique-id、MUSA context 创建、或节点间握手资源。
- **两者差异**：
  1. **进程模型**：mpirun 拉的是 **16 个完全独立进程**（各自独立的 MUSA context、独立的 NVSHMEM 初始化、独立通信资源申请）；sglang 是**单进程 + `torch.multiprocessing.spawn` 派生子进程**，父子/兄弟进程的初始化顺序与资源共享受框架协调，更温和。
  2. **时序**：mpirun 让 16 个进程**几乎同时启动、同时初始化**，很可能同时去抢同一有限资源（QP 数、bootstrap 等）而触发 EAGAIN；spawn 子进程通常是**有先后的时序**。
  3. **GPU 绑定与上下文**：mpirun 下 `set_device(local_rank)` 需各进程正确绑定（取决于 rank↔device 映射是否正确）；spawn 由框架保证。

- **结论**：记录自己的假设「**spawn 的时序 vs mpirun 的同时启动**」很可能就是关键变量，但**当时未验证**。建议实测确认：逐个/加退避启动 16 进程、查 EAGAIN 具体返回点（是否 NVSHMEM 初始化、是否 IB QP 申请）、或用 `srun` 的进程启动顺序控制，把「时序竞争」与「资源不足」区分开。**此结论为待验证假设，非定论。**

---

## 附：模型优化点 / 未解决项清单（跨案例）

- all-reduce skew：TP1（prefill）/ TP7（decode）固定拖后腿 → 待研究 skew 来源。
- device 7 CPU 空洞：inter-rank 调度器同步延迟 → 待研究。
- SBO：mate 缺 `num_mp` 暴露 + DeepEP combine MUSA 二进制无 overlap → 需自研。
- mpirun EAGAIN → 待实测验证（时序竞争 vs 资源不足）。
- 64k 上下文输入存在 bug（三套参数均失败）。
