# 07 互连与网络技术

> 来源：`手记.md` 6.25、7.22、7.29–7.30、8.10–8.12、8.17、8.19、8.25–8.27
> 参考：工作区 `ace_mte_doc/`（ACE/TME 实测调研）、`cuda&musa调研.md`、`IBGDA详解.md`
> 关联：[05 GPU 硬件与编程基础](./05_GPU硬件与编程基础.md)、[08 DeepEP与MoE通信算子](./08_DeepEP与MoE通信算子.md)

---

## 1. 互连总线

- **PCIe**：一种总线，通用性强，可连接各种设备（CPU、GPU、SSD…）。
- **NVLink**：GPU 之间通信，带宽远高于 PCIe、延迟更低；配合 **NVSwitch** 做多 GPU 全互联。
- **MTLink**（摩尔线程）：对标 NVLink 的卡间直连链路（8 卡全连接带宽可达 784 GB/s，S5000 单卡显存带宽 1.6 TB/s）。

---

## 2. RDMA 与 IBGDA

### 2.1 基本概念

- **RDMA（Remote Direct Memory Access）**：远程直接内存访问，通过 InfiniBand 网络进行 GPU 之间的**跨节点**通信。
- **IBGDA（InfiniBand GPUDirect Async）**：NVSHMEM 提供的关键特性，**允许 GPU 直接发起 RDMA 操作，无需 CPU 介入**。
- **IPC P2P（进程间 peer-to-peer，GPU 间直接访问）**：需通过 NVLink、PCIe P2P、NVSwitch 互联。
- **QP（Queue Pair）**：RDMA 里用于收发数据的一对队列（发送队列 + 接收队列）。

### 2.2 传统 RDMA 的 CPU 瓶颈 vs IBGDA

传统流程（CPU 中介）：GPU kernel 完成 → CPU `cudaMemcpy` 同步 → CPU `ibv_post_send` → NIC 执行 RDMA → CPU 轮询 CQ → 通知 GPU。每次通信都要 **GPU↔CPU↔GPU 跨设备同步往返**。

**IBGDA 解决另一半**：让 **GPU kernel 自己写 doorbell、自己提交 RDMA 请求**。两大支柱：
1. **GPUDirect RDMA（P2P DMA）**：NIC 的 DMA engine 直接读写 GPU 显存，数据不经过 CPU 内存。
2. **GPUDirect Async（核心）**：把 IB 网卡的 **UAR（User Access Region）** 寄存器空间映射到 GPU 虚拟地址空间，WQ 等 verbs 数据结构放在 GPU 显存，GPU kernel 可直接写 UAR doorbell。

关键组件：**UAR**（每 QP 关联的 MMIO 寄存器区）、**Doorbell**（写 UAR 的固定格式数据，含 QP 号/WQE 数）、**WQ/WQE**（工作队列/64B 请求描述符）、**CQ/CQE**（完成队列/完成条目）。

**⚠️ doorbell 内存序**：WQE 在 GPU 显存、doorbell 是 PCIe write，走不同 memory domain，默认 store 顺序不保证二者对 NIC 的可见顺序。需 `st.release`（release 语义）或 fence 保证「WQE 先落、doorbell 后发」。

### 2.3 RoCEv2 与 DCQCN（8.27）

跨节点走 **RoCEv2（RDMA over Converged Ethernet）**——把 RDMA 跑在以太网交换机上。RDMA 假设「网络不丢包/拥塞」，但以太网交换机有缓冲队列。**DCQCN（数据中心量化拥塞通知）**负责告诉发送方「慢点」：

```
突发（一次提交多笔 WQE）→ 交换机队列超阈值 → 给数据包打 ECN 标记
→ 接收方 NIC 反向发 CNP（拥塞通知包）→ 发送方 NIC 把对应 QP 速率砍到接近 0
→ 再按算法慢恢复（"砍零 + 慢慢爬"周期 = 文档里 74ms 停顿）
```

### 2.4 RoCE 与 InfiniBand 的关系（❓答疑）

> 记录 9.4 的问题。一句话：**RoCE 不是 IB 的一种形态，而是「借 IB 的上层协议与 RDMA 接口、把数据平面换成以太网」——同一套 RDMA 栈，两种物理网络。**

**两者共用的部分**（所以上层软件基本无感）：
- 同一套 verbs 接口：QP/WQ/CQ、doorbell、`ibv_post_send`、轮询 CQ（§2.2 全适用）。
- 同一套 IB 传输层报文格式（BTH、PSN…）：RoCEv2 只是把 IB 报文再封装进 **UDP/IP（默认端口 4791）**，RC 可靠传输语义不变。
- 同样支持 GPUDirect RDMA；很多网卡一颗芯片双模式（IB 口 / 以太网口）。

**不同的部分——「无损」从哪来**：

| | InfiniBand | RoCE |
|---|---|---|
| 网络 | **专用** IB 交换机/HCA | **现成以太网**交换机 |
| 不丢包 | 链路级 credit 流控**内建**，天然无损 | 以太网默认尽力而为 → 靠 **PFC**（优先级流控）划出无损通道 |
| 拥塞控制 | 协议原生 | **DCQCN/ECN** 补课（见 §2.3） |
| 管理 | Subnet Manager | 普通 IP 网络 |
| 成本 | 高 | 低 |

**核心逻辑**：RDMA 可靠传输（RC）假设「底层不丢包」，否则重传性能崩盘。IB 靠内建流控天然满足；RoCE 跑在以太网上就靠 PFC/DCQCN 凑出这个前提——**RoCE ≈ IB 上层协议 + 靠 PFC/DCQCN 模拟出的「类 IB 无损」**，这也是 §2.3 DCQCN 存在的原因。

**版本**：
- **RoCEv1**：IB 报文直接封装进以太网帧（EtherType 0x8915），L2 不可路由，基本废弃。
- **RoCEv2**：封装进 UDP/IP，可跨三层路由，实践中基本都用 v2。

**三条 RDMA 路线（更完整的图）**：InfiniBand（IB 专用网络）/ **RoCE（IB 传输层 + 以太网，主流）** / iWARP（RDMA over TCP，连传输层都换，性能差、基本被取代）——三者上层都表现为同一套 verbs 接口，「换底层、留接口」。

---

## 3. NVSHMEM / NCCL / multimem

- **NVSHMEM**：一套**多 GPU / 多节点**的通信编程库/API，处于软件层。
- **NCCL vs NVSHMEM**：NCCL 更像「大家一起做 collective」；NVSHMEM 更像「某个 GPU 主动读/写另一个 GPU 的内存」。
- **NVSHMEM bootstrap**：NVSHMEM 初始化阶段的「进程发现和信息交换后端」。
- **multimem**：一个「特殊地址」同时代表多块 GPU 上的多份物理内存（scale-up 域，见 [06](./06_分布式并行.md)）。

### 3.1 NVSHMEM 对称堆

每个 PE（rank）各自在自己 GPU 上分配**等大、同偏移**的内存块，逻辑布局对称（offset 一样），可根据本地偏移算对端地址，通过网卡（NIC）直接写到别的 rank 的 recv 区 buffer 显存。

---

## 4. TME / ACE / TMA 的关系（❓答疑）

> 记录 8.10 的问题，据 `ace_mte_doc/` 调研给出。

**💡 解答**：摩尔线程第四代 MUSA「平湖」架构有三大引擎（计算-访存-通信协同）：

| 引擎 | 全称 | 职责 | NVIDIA 对标 |
|---|---|---|---|
| **TCE** | Tensor Compute Engine | 张量计算（MMA 矩阵乘） | Tensor Core |
| **TME** | Tensor Memory Engine（张量访存引擎） | **设备内 global↔local（shared）大块张量搬运、数据布局转换** | **TMA** |
| **ACE** | Async Copy Engine | 跨内存域异步通信（Host↔显存、GPU↔GPU、Die↔Die） | Copy Engine + NVLink + NVSHMEM 远程原子（三者合并） |

**TMA vs TME**：TMA 是 Hopper 的「单设备内部 global↔shared」异步搬运指令（`cp.async.bulk.tensor`）；**TME 就是 MUSA 的 TMA 对标物**，官方名称即 TME（"TMX" 非官方术语）。

**ACE vs TMA/TME 的分工（关键）**：
- **TME**：只管**设备内** global↔shared 这条 SM 内通路，指令级（kernel 内 `__musa_tme_ld_tile_*` + 描述符 `muTensorDescriptorEncode` + mutlass/mute 库）。
- **ACE**：管**跨域**（Host↔显存、GPU↔GPU、Die↔Die），库级（MCCL：`mcclAceCommInitRank` + 对称窗口 + 集合调用），**SM 不参与通信**，适用集合通信（如 AllReduce）。ACE 不管 global↔shared。

**TME 软件栈三层**（`ace_mte_doc/06_TME调研.md`）：
1. **驱动 API**：`muTensorDescriptorEncode`（对标 `cuTensorMapEncodeTiled`）；
2. **编译器内建（mcc）**：`__musa_tme_ld_tile_{1d..5d}` / `st_*` / `store_commit` / `tensor_tile_reduce`（MP31 专属指令）；
3. **头文件库**：mutlass / mute（CUTLASS/CuTe 的 MUSA 移植）。

### 4.1 新版本 SDK 用 TME 的方式是不是不一样（❓答疑）

> 记录 8.12 的问题。

**💡 解答（据调研 + 标注待确认）**：
- TME 三层栈在本机（SDK 4.3.5 / S5000）**均已存在且实测通过**（1D/2D/3D/swizzle/store/OOB 5/5 通过）。
- 头文件里还留着**被注释掉的更完整 `MUtensorMap`（16×QWord）** 与 `muTensorMapEncodeTiled/Im2col/ReplaceAddress`，说明 API **仍在演进**——「新版本 SDK 用法是否不同」需对照新版头文件确认，不能仅凭旧版推断（与 8.27「代码不涉及不代表做不到，实测才知道」是同一原则）。
- 实测陷阱：`MU_DEVICE_ATTRIBUTE_TENSOR_MAP_ACCESS_SUPPORTED(127)` 查询**返回 0（误导）**，但 TME 实际可用——能力探测不能依赖该属性，应以实测为准。

---

## 5. MTSHMEM P2P vs IPC P2P vs IBGDA

### 5.1 三方案横向对比（8.17）

| | MTSHMEM P2P（MTLink） | IBGDA（InfiniBand） |
|---|---|---|
| 传输方式 | GPU SM 发起 store 指令，直接写对端 GPU 显存 | GPU SM 填 WQE（doorbell），NIC 做 RDMA put |
| 硬件上限 | MTLink DMA 108 GB/s（SM 拷贝实测 ~56 GB/s） | IB 网卡 25 GB/s/卡 |
| 延迟特征 | 同步——store 完成即到达对端 | 异步——put_nbi 提交后不等完成，可与后续操作重叠 |
| 拷贝主体 | SM 指令（占用算力） | NIC DMA 引擎（不占 SM） |

### 5.2 mtshmem 与 IPC：同链路、两套软件机制

两者**都走 MTLink 物理链路**，但软件机制完全不同：

- **mtshmem P2P（内核实际用的）**：初始化时用 `musaDeviceEnablePeerAccess` 开启同节点 GPU 间 peer access，再通过统一虚拟寻址（UVA）把每个 rank 的对称堆映射到本地 GPU 地址空间（存于 `peer_heap_base_p2p[dst_rank]`）。内核拿地址后用 **SM 的 store 指令直接写**，数据经 MTLink 到对端 GPU 显存。这是 NVSHMEM 标准 P2P 机制（`nvshmemi_get_p2p_ptr` 查此表）。
- **IPC P2P（探索过、已放弃的替代方案）**：完全不同的 API——`musaMalloc` 分配普通显存 → `musaIpcGetMemHandle` 拿句柄 → 进程间交换句柄（文件或 MPI）→ `musaIpcOpenMemHandle` 打开对端映射 → 内核写此映射地址。

### 5.3 ACE 与 mcclCommWindowRegister（8.12）

- **ACE 只需在 host 侧调用**（库级，SM 不参与通信）。
- **`mcclCommWindowRegister`**：把一块显存「注册」到所有 GPU 可见的对称地址空间中。

---

## 6. 缓冲与原子语义

### 6.1 out-of-place vs in-place

- **out-of-place**：集合通信中，输入和输出是两个不同缓冲区——从 sendbuf 发，写到 recvbuf。
- **in-place**：结果直接写回输入缓冲区本身。

### 6.2 原子操作

对内存的一次不可分割的「读-改-写」：硬件保证这三个步骤作为一个整体完成，中间不会被其他访存操作插入。

### 6.3 atomicCAS（compare and swap）

```
CAS(地址, 期望值 old, 新值 new):
    如果 *地址 == old:  把 *地址 写成 new, 返回 old（成功）
    否则:               什么都不改,       返回内存里的实际值（失败）
```

**无锁编程的基石**。类比银行叫号机：看到 7 号就按「拿 8 号」——没人动过（还是 7）就给你 8 号；被人抢先（已变 8）就失败重看。CAS 把「看一眼 + 更新」合成一个不可分割的原子操作，多线程/多 warp 抢同一计数器时不会两人拿到同一号。

### 6.4 RDMA staging

每张卡为通信**预先分配的暂存 buffer**（即对称堆地址），包括 send buffer、recv buffer 等；每个 rank 各有一块对称堆。

---

## 7. 系统讲解 MUSA 与 CUDA 的关系（❓答疑）

> 记录 8.10 的问题，据 `cuda&musa调研.md`。

**💡 解答**：

**相同点（编程模型几乎一一对应）**：
- 都是「Grid–Block–Thread」三级结构 + SIMT 执行 + `kernel<<<...>>>` 启动。
- 内存层次一致（寄存器/共享/本地/全局/常量/纹理/统一内存）。
- 同名同步与原子函数（`__syncthreads`、`__threadfence`、`atomicAdd/Exch/CAS`）。
- 代码迁移基本就是 `cuda*` → `musa*` 前缀替换（MUSA 官方宣称对 CUDA「无缝替换」，有 `musify` 自动转换工具）。

**不同点（硬件与生态）**：

| 维度 | CUDA | MUSA |
|---|---|---|
| 计算单元 | SM（Streaming Multiprocessor） | MP（MUSA Processor） |
| warp 大小 | 32 | 设备可定：S5000=32，老卡（M1000/S4000）=128 |
| 加速引擎 | Tensor Core、TMA | **TCE / TME / ACE**（三大引擎） |
| 编译器 | NVCC | mcc |
| 数学库 | cuBLAS/cuFFT/cuDNN | MUSA-X（muBLAS/muFFT/muDNN） |
| 通信库 | NCCL | MCCL |
| 分析工具 | Nsight Systems/Compute、cuda-gdb | MoorePerf、muPTI |
| 迁移工具 | — | musify（CUDA→MUSA） |
| 多任务隔离 | MPS / MIG | Green Context（创建上下文时预留 MP 资源） |

**结论**：CUDA 生态成熟、性能极致；MUSA 走国产自主可控、与 CUDA 接口兼容的路线，**代码通过源码兼容跨平台，不能在对方硬件上原生运行**。迁移坑点：warp 大小差异、数学库/指令集（TCE 张量核与 CUDA Tensor Core 不完全等价）需逐步验证。
